Fetch content from a URL and optionally convert to markdown.
- Reads web pages, raw text, HTML content
- Can return as markdown (default), text, or HTML
- Useful for reading documentation, API specs, or external resources
- Not for search - use websearch for that